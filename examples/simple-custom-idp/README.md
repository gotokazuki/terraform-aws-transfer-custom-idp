# Simple custom idp example

The example creates a simple custom idp for Transfer Family

## Usage

```console
$ terraform init
$ terraform plan
$ terraform apply
```

## Outputs

|Name|Description|
|:-:|:-:|
|apigw_deployment_invoke_url|The invoke url used by IdP of Transfer Server|
